Version 1.1.1
    Fixed issue where the "window always on top" option didn't load from saved config
    Fixed issue where the timer & window wouldn't reset when the clock reset

Version 1.1
    added color pickers to the settings menu
    added automatic window expansion
        increases window size based on time (hours -> wider)
    stopwatch window is now always above other windows (unless disabled in settings)
     renamed "settings" window to "options"
    replaced options titlebar with default window titlebar
    removed clock border which offset the text too far
    fixed bug where times from 1 hour onward were displaying incorrectly